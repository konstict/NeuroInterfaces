Client - клиентская часть (Локальная программа для водителя - отображение состояния в реальном времени)  
Server - серверная часть (Удаленная программа для диспетчера – мониторинг водителя по ID)

# Source (исходные файлы проекта):
### uis:
Выполненые формы в PyQt designer, а затем преобразованы в НазваниеОкна.py для их использования в основном файле main.py
### files:
Фотографии, видео и аудио файлы

# Client:
### main.py:
Основной файл проекта
### clientSender.py:
Скрипт для отправки серверу operators_db.csv (база данных операторов в текстовом формате)  
и содержимое папки operators (хранятся фотографии лиц операторов)  
по IP адресу и порту (в локальной сети)
### arduino / arduino.ino:
Для детекции QRS комплекса (удара сердца) с использованием ЭКГ датчика и дальнейшего вычисления пульса (количества ударов в минуту) использовались некоторые шаги в своей последовательности из Алгоритма Пана – Томпкинса.  
А именно 3, 4 и 5 шаги [https://engineering.pnzgu.ru/files/engineering.pnzgu.ru/10(1).pdf].  
Данный алгоритм реализован в .ino файле на Arduino C/C++. В основном выглядит следующим образом: 
1) Получение значения с ЭКГ датчика
2) 5й шаг - для сглаживания небольших шумов (усредняются 5 значений)
3) 3й шаг - для получения независимых значений (операция дифференцирования возвращает скорость изменения этого сигнала и это очень подходит для дальнейшего взятия константного порогового значения)
4) 4й шаг - взятие в квадрат данного значения для того, чтобы не была важна полярность и все предыдущие значения которые были на отрицательной линии - становятся на равне с положительными, и все высокие значения всё больше и больше становятся заметнее, в сравнении с низкими благодаря самой операции возведения в квадрат
5) Снова выполняется 5й шаг для ещё одного сглаживания (усреднения) просто потому-что график выходит более эффективней
6) А затем единажды определяется константное значение порога, при котором будет определятся каждый «удар сердца»
### Форма Управление:
Состояние норма:
<img width="1013" height="513" alt="image" src="https://github.com/user-attachments/assets/4d98ef23-6194-4c5c-a87c-29127fd6c3f5" />
Состояние внимание:
<img width="1013" height="514" alt="image" src="https://github.com/user-attachments/assets/bc898dd4-e372-4169-8900-2574c088fb2b" />
Состояние критично:
<img width="1024" height="517" alt="image" src="https://github.com/user-attachments/assets/93eb8d76-c52d-41dd-a6f6-d254d4872c45" />


# Server:
### main.py:
Основной файл проекта
### serverRecver.py:
Скрипт для получения с клиента operators_db.csv (база данных операторов в текстовом формате)  
и содержимое папки operators (хранятся фотографии лиц операторов)  
по IP адресу и порту (в локальной сети)
Вид главной формы во время передачи данных о операторов с клиента на сервер:
<img width="1000" height="506" alt="image" src="https://github.com/user-attachments/assets/d65387e0-7476-42c6-868e-ee6e3b849ca8" />
